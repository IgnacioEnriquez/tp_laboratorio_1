#ifndef OPERACIONES_H_INCLUDED
#define OPERACIONES_H_INCLUDED



#endif // OPERACIONES_H_INCLUDED

/** \brief Realiza la suma dos numeros flotantes y las almacena en una variable
 *
 * \param Variable donde se guardara la suma
 * \param Primer numero a sumar
 * \param Segundo numero a sumar
 *
 */
void sumar(float *a,float b,float c);

/** \brief Realiza la resta de dos numeros flotantes y las almacena en una variable
 *
 * \param Variable donde se guardara la resta
 * \param Primer numero a restar
 * \param Segundo numero a restar
 *
 */
void restar(float *a, float b, float c);

/** \brief Realiza la division de dos numeros flotantes y las almacena en una variable
 *
 * \param Variable donde se guardara la division
 * \param Numero a dividir
 * \param Numero divisor
 *
 */
void dividir(float *a, float b, float c);

/** \brief Realiza la multiplicacion de dos numeros flotantes y las almacena en una variable
 *
 * \param Variable donde se guardara la multiplicacion
 * \param Primer numero a multiplicar
 * \param Segundo numero a multiplicar
 *
 */
void multiplicar(float *a,float b,float c);

/** \brief Realiza el factorial de un numero
 *
 * \param Numero al que se quiere realizar el factorial

 * \return Resultado de el factorial del numero
 *
 */
double factorizar(double b);
